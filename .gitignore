node_modules/
dist/
.wrangler/
.dev.vars*
.env*
!.env.example
*.log

# 실제 사업 자료와 개인 이력. 이 저장소는 공개이므로 올리지 않는다.
# 지원서·계획서에는 실명·학력·경력·기관 정보가 들어간다. 한 번 올라가면 지워도 이력에 남는다.
제안서/
# 프로젝트 전체를 한 파일로 뜬 것. 소스가 통째로 중복되고 용량이 크다.
snapshot.md
