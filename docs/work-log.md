# 작업 목록

작업 번호로 어디까지 왔는지 본다. 한 작업이 한 줄이다.

상태는 넷 중 하나다.

- **조사 완료** — 코드를 건드리지 않았다
- **수정 완료** — 커밋했으나 배포하지 않았다
- **배포 완료** — 운영(pro.ms12.org)에 올렸다
- **중단** — 문제가 있어 멈췄다

`main`에 푸시하면 Cloudflare Pages가 곧바로 운영에 배포한다. 그래서 「수정 완료」와
「배포 완료」를 가르는 것은 푸시 한 번이다.

## 일하는 방식 (2026-08-22부터)

조사와 수정을 나누지 않는다. 조사해서 답이 나오면 그 자리에서 고치고 `npm test` 후 배포까지 한 뒤
보고한다. 「조사만 하라」는 말이 지시에 없으면 기본은 배포까지다.

보고에는 다섯 가지를 담는다 — 무엇이 문제였고 원인이 무엇인가, 어떻게 고쳤고 다른 방법을 왜 고르지
않았나, 전·후 숫자, 시험을 무엇으로 고정했나, 걸리는 것(확신이 안 서는 판단과 미룬 것).

**멈추고 묻는 네 가지.** 실제 AI 호출로 실측이 필요할 때(비용), DB 마이그레이션이 필요할 때,
되돌리기 어려운 변경일 때, 제품 판단이 필요할 때. 이때는 고치지 말고 묻는다.

## 2026-08-22에 운영에 올린 것

번호 없이, 무엇이 달라졌는지만 적는다.

- **연혁 HWP를 올리면 실적이 들어온다.** 표의 행이 사라져 후보가 0건이던 것이 99건이 됐고,
  98건은 발주기관과 사업명이 원문 그대로 들어온다.
- **사업자등록증·고유번호증을 올리는 자리가 기본정보 카드 맨 위에 생겼다.** 기관명·대표자·소재지·
  고유번호가 자동으로 채워진다. 다만 글자가 있는 PDF만 읽는다.
- **실적 96건이 화면을 뒤덮지 않는다.** 해마다 접히고, 한 건은 한 줄이며, 눌러야 편집칸이 열린다.
- **「모두 확인」이 실적 제목 줄에 있다.** 96번 누르던 일을 한 번에 하고, 잘못 눌렀으면 되돌린다.
- **화면 맨 위에 「다음 할 일」 띠가 생겼다.** 지금 해야 할 한 가지와 그 버튼만 있고, 갈색 버튼은
  그 하나뿐이다. 지금 손댈 자리는 초록(네이버 초록)으로 표시하고 화살표가 네 번 깜박인다.
- **공고와 관련된 실적만 계획서 요청에 실린다.** 기관 자료가 매 호출 9,953자에서 1,255자로 줄었고,
  나머지는 「그 외 96건」으로 건수만 간다.
- **「이 공고와 겹치는 실적 3건 / 전체 99건」을 비교 화면에서 볼 수 있다.** 무슨 낱말로 걸렸는지도 적는다.
- **공고 요구를 「기관정보에 없는 사항」에 몰아넣지 않는다.** 수행 요구는 「계획서에 답해야 할 요구사항」
  으로 가고, 앞 항목을 묶은 요약 문장에는 꼬리표가 붙는다.
- **담당자·주로 돕는 대상·강점을 올린 자료에서 제안한다.** 어디서 온 값인지 함께 적고, 확인 필요 상태다.
- **오래된 기관 정보에 「다시 확인」이 뜬다.** 상태는 바꾸지 않고 표시만 하며, 그 자리에서 새 문서를 올린다.
- **문서 날짜를 개업연월일로 잡던 것을 고쳤다.** 발급일·출력일이나 문서 끝의 날짜를 쓰고, 없으면 올린 날짜다.
- **기관이 하나면 기관 고르는 칸이 한 줄로 준다.** 공고가 컨소시엄을 요구하면 그때는 펴 둔다.
- **손으로 넣는 폼이 접혀 있다.** 아홉 구역에 늘 펼쳐져 있던 입력칸 45개가 줄 아홉 개가 됐다.

## 2026-08-22 오후에 더 올린 것

- **기관이 준 서류 원본을 보관한다.** 기관자료 줄에 파일을 붙이고, 열고, 지운다. 지울 때는 되돌릴 수
  없다고 먼저 묻는다. 파일은 그 기관을 볼 수 있는 사람만 연다.
- **찾은 값은 묻지 않고 넣는다.** 담당자·주로 돕는 대상·강점이 바로 들어가고 출처 한 줄만 남는다.
- **기본정보도 적는 대로 저장된다.** 「자동 저장 중」이 이제 사실이다. 저장 버튼은 없앴다.
- **문서 날짜를 개업연월일로 잡던 것을 고쳤다.** 발급일·출력일이나 문서 끝의 날짜를 쓰고, 없으면 올린 날짜다.

- **공고 조항에 실적이 통째로 붙던 것을 고쳤다.** 항목명이 아니라 값으로 판정하고, 계획서에 실을
  실적을 고르는 규칙을 그대로 쓴다. 「기간이 조정될 수 있다」에 붙던 99건이 2건이 됐다.

## 나중에 이어서

- **서류 삭제** — 즉시 삭제로 결정(2026-08-22). 되돌리기 필요가 확인되면 그때 재검토한다.
  「지웠는데 서버에 남아 있다」가 더 위험하고, 개인정보 삭제 요구가 오면 유예가 곤란하며,
  되돌리기가 필요할 만큼 실수가 잦은지 아직 모른다. 대신 지울 때 되돌릴 수 없다고 한 번 묻는다.

## 지시를 받았지만 아직 하지 않은 것

- 없음. 받은 번호는 모두 끝냈다. 22-10 · 22-20 · 22-26 · 22-29 번호는 지시를 받지 못했다.

## 조사만 하고 멈춘 것

- ~~서류 파일 보관(R2)~~ — 2026-08-22에 만들어 배포했다. 유예 삭제만 남았다(위). 조사는 `docs/file-storage-r2.md`.
- **사진·스캔본 읽기(OCR)** — 다사108 조사. 지금 가진 등록증 넷 중 셋이 스캔본이라 글자가 0자다.
  OpenAI 이미지 입력으로 장당 10원 안팎. 크기 300~400줄, 1.5~2일.

## 알고 있는데 아직 고치지 않은 것

크기는 만드는 데 걸리는 시간이다.

- **사진·스캔본을 못 읽는다.** 마인드스토리 사업자등록증 본인 것이 스캔본이라 지금은 한 글자도
  못 읽는다. 홈택스에서 PDF로 새로 받으면 읽힌다. — 1.5~2일
- **요구사항 중복 세기.** 「필수사업 다섯 가지를 모두 포함해야 한다」에 꼬리표는 붙지만 숫자는 여전히
  6건이다. 합치지 않기로 한 결정이라 숫자를 줄이려면 따로 정해야 한다. — 반나절
- **한 줄에 두 항목이면 뒤엣것을 버린다.** 「개업연월일 … 법인등록번호 …」처럼 한 줄에 둘이 찍히면
  뒤 항목의 값이 저장되지 않는다. 나누는 규칙은 서식마다 순서가 달라 잘못 나누면 값이 뒤바뀐다. — 하루
- **기관 유형은 손으로 골라야 한다.** 사업자등록증에 그 칸이 없기 때문이다(고유번호증의 「단체유형」은
  읽는다). 세무상 「법인사업자」를 기관 유형으로 옮기면 틀린 값이 된다. — 고칠 것 없음
- **단계 번호가 화면마다 다르게 보인다.** 작업 화면은 여섯 단계를 0부터 세고 홈 화면 요약도 같은
  번호를 쓰는데, 사용자 화면에서 「02」와 「5」가 함께 보였다는 보고가 있다. 아직 재현하지 못했다. — 조사 반나절
- **완료 띠와 단계 표시가 어긋난다.** 위와 같은 자리에서 나온 보고이고 함께 봐야 한다. — 조사 반나절
- **이미 반영해 둔 실적 99건에는 「프로그램 내용」이 비어 있다.** 그 칸은 나중에 생겼다. 연혁을 다시
  올려 반영하면 채워지고, 같은 값은 줄이 늘지 않는다. — 사용자 조작 한 번
- **컨소시엄 판정이 낱말 규칙이다.** 「2개 이상 기관」·「컨소시엄+필수」는 읽지만 「단독 신청 불가」처럼
  다르게 쓰면 못 읽는다. 실제로 그런 공고를 만나면 그 표현만 더한다. — 실물 보고 판단
- **「다시 확인」 기준 두 해는 임의로 정한 값이다.** 실제로 써 보고 잦거나 뜸하면 숫자만 바꾼다. — 한 줄
- **늘 펼쳐진 빈 입력칸이 아직 여섯 곳 있다.** 기관자료 등록 폼(4칸)·아이디어 자산 패널(6칸)·이번 사업
  전용 값(3칸)·참고자료(5칸)·관리자 권한 지정(10칸)·프리미엄 이력 검색(4칸). 앞의 둘은 접는 편이 낫고
  참고자료는 자주 쓰는 길이라 두는 편이 낫다고 본다. — 각 반나절
- **분석 프롬프트에 「같은 요구를 두 번 만들지 마라」가 없다.** 고치려면 실제 AI 호출로 재야 해서
  비용이 든다. 받은 뒤 표시하는 쪽(22-15)으로 갈음했다. — 실측 필요, 지시 대기
- **브라우저 저장 한도를 넘으면 자동 저장이 조용히 실패한다.** 콘솔에만 남고 화면에는 알리지 않는다.
  참고자료처럼 큰 원문을 여러 개 넣으면 생길 수 있다. — 반나절

## 기록

- **다사107** · 배포 상태를 확인하고 실제 기관 연혁 HWP 4건으로 추출을 실측 · 조사 완료 · 커밋 없음 · 배포 없음(이미 올라가 있던 021c8af를 라이브 번들에서 확인) — 연혁에서 후보 0건임을 확인하고 원인이 표 행 소실임을 짚음
- **다사108** · 서류 첨부 자리·안내·끌어다 놓기·사진 OCR 네 갈래 조사 · 조사 완료 · 커밋 없음 · 배포 없음 — 1단계 「여기에 놓기」에 드롭 처리기가 없다는 것과 사업자등록증의 「상호·성명」 규칙 구멍을 찾음
- **다사109** · HWP 표 행 복원(레코드의 행·칸 번호 사용)과 실적 끝말 조건을 연도+발주기관 근거로 대체 · 배포 완료(다사110에서) · `142e889` · 연혁 후보 0 → 99건, 98건이 기관·사업명 온전, 오탐 불변
- **다사110** · 다사109를 운영에 배포 · 배포 완료 · `142e889` · 운영 번들 `index-B4g3kaRX.js`에서 확인
- **다사111** · 후보가 생긴 자리로 스크롤·강조, 다음에 누를 버튼 표시, 끌어다 놓기 배선, 들어가는 문 문구 교체 · 배포 완료 · `99d43f4` · 다사113① 배포에 함께 올라감 — 함께 조사: 반영된 실적이 계획서 프롬프트로 가는 경로와 크기(508자 → 9,953자)
- **22-25①** · 기록할 자리 앞 화살표(네 번 깜박이고 멈춤, 초록과 같은 판정·같은 클래스) · 배포 완료 · `be18988`
- **22-24** · 초록(#03C75A)을 「다음 할 일」이 가리키는 한 곳에만 — 버튼이면 초록 버튼, 아니면 초록 테두리+글자 · 배포 완료 · `096e38a` · goMark 한 통로로만 붙고, 통로를 지나지 않은 초록이 있으면 시험이 실패한다
- **22-23** · 「주로 돕는 대상」 근거를 발주기관 이름과 프로그램 내용까지 넓힘 · 배포 완료 · `8235e3b` · 「아동 8회」 → 「학생 38곳 · 아동 8곳 · 다문화 7회」. 실적 항목에 「내용」 칸을 만들어 실적표의 프로그램 내용이 반영 때 버려지지 않게 했다
- **22-21·22-22** · 기본정보 세 칸을 올린 자료에서 제안(출처 표시·확인 필요·연락처 제외), 기관이 하나면 기관 고르는 칸을 한 줄로(컨소시엄 요구면 폄) · 배포 완료 · `411326b` · 22-19 접기가 안 먹은 까닭도 함께 고쳤다 — 펼침 기억을 브라우저에 저장해 새로고침해도 펴져 있었다. 이제 세션 한정이다
- **22-19** · 손 입력 폼 접기·이름 바꾸기(「문서에 없는 것을 손으로 넣기」), 일괄 반영을 초록으로, 띠가 후보를 먼저 가리키게 · 배포 완료 · `5862e68`+`8fc1aa2` · 입력칸 45개가 줄 아홉 개로 줄었고, 등록증을 갓 올린 기관에서 띠가 「후보 반영하러 가기」로 바뀐다
- **22-18** · 값에 섞여 들어온 라벨 부스러기 떼어 내기(앞뒤 콜론·괄호 라벨·자간·뒤에 붙은 다음 칸 라벨) · 배포 완료 · `002d236` · 설립 시기 「: 2021 년 08 월 26 일 법 인 등 록 번 호 :」 → 「2021년 08월 26일」. 한 줄에 두 항목을 나누는 일은 하지 않았다
- **22-17** · 기본정보 카드에 등록증 올리는 자리 + 상호·성명 규칙 + PDF 줄·칸 복원 · 배포 완료 · `74f7d00` · 사업자등록증명원에서 후보 1건 → 4건(기관명·소재지·대표자·고유번호). 디스크의 등록증 넷 중 셋은 스캔본이라 글자가 0자 — OCR 몫으로 남았다
- **22-16** · 일하는 방식 바꾸기(조사·수정·배포를 한 번에) · 배포 완료 · `기록만` · 위 「일하는 방식」에 적었다
- **22-15** · 앞 항목을 묶은 요구사항에 꼬리표 달기(`src/requirement-rollup.js`) · 배포 완료 · `461683b` · 합치지도 지우지도 않고 건수도 그대로 두며, 그렇게 본 까닭을 함께 적는다
- **22-02(새 번호)** · 요구사항 중복 세기 · 조사 완료 · 커밋 없음 — 분석 프롬프트에 중복 금지 지시가 없고 요구사항 스키마에 「묶음」을 적을 칸도 없다. 공고 원문에는 묶음 문장이 거의 없고(553줄 중 1건, 그마저 오탐) 요약은 AI가 만든다. 받은 뒤 표시하는 쪽이 실측 없이 되고 이미 저장된 분석에도 듣는다
- **22-13·22-14** · 없는 단추를 가리키던 안내, 새로고침 뒤에도 남던 「방금」, 화면 문구에 박힌 기관명, 그리고 「모두 펼치기」·「이 기관 정보 저장」 제거 · 배포 완료 · `c11c9dc` · 알림 문구가 저장되던 것이 「방금」의 진짜 원인이었다
- **22-11** · 한 화면에 주 버튼 하나 · 배포 완료 · `12b152b` · 22-12에서 다섯 개를 흰 버튼으로 내렸고, 남은 중복(「실적 N건 모두 확인됨으로」)을 빼 제목 줄 단추 하나로 합쳤다. 시험이 갈색 버튼 두 개 이상이면 실패한다
- **22-12** · 화면 맨 위 「다음 할 일」 띠와 판정 함수 하나(`src/org-next-step.js`) · 배포 완료 · `fc95915` · 이 화면 주 버튼이 아홉 자리에서 띠 안의 하나로 줄었다
- **22-06·22-08·22-09** · 실적 목록을 연도별·건별로 접고, 자료 있는 묶음을 펼치고, 「모두 확인」을 제목 줄로 · 배포 완료 · `bad15c9`+`200ec87` · 96건이 열 줄로 줄었다
- **22-07** · 신청기관 화면의 누를 수 있는 요소 세기 · 조사 완료 · 커밋 없음 — 처음 화면에서 눈에 보이는 것 37개, 접힌 것까지 95개, 지금 사용자 상태로는 약 436개. 반드시 눌러야 하는 것은 2개
- **22-05** · 22-04 배포 + 22-03 수리(수행 요구를 「계획서에 답해야 할 요구사항」으로) · 배포 완료 · `4375c15` · 실제 공고에서 다섯 줄이 기관정보 칸에서 빠져 새 칸으로 옮겨감
- **22-04** · 실적 일괄 확인(어제 113②)과 되돌리기 · 배포 완료 · `80f2d32` · 22-05에서 함께 올림 — 99건을 한 번에 확인됨으로 올리고 같은 자리에서 되돌린다
- **22-03** · 「기관정보에 없는 사항」이 등록할 값이 아닌 문제 · 조사 완료 · 커밋 없음 — 「~해야 한다」로 끝나는 수행 요구가 갈 곳이 없어 이 칸으로 떨어진다. 화면 분류만의 문제이고 계획서로 나가지는 않는다
- **22-02** · 화면에 같은 카드가 여러 번 나오는지 확인 · 조사 완료 · 커밋 없음 — 세 카드 모두 한 번만 그린다(어제 다사116과 같은 조사)
- **다사114** · 「이 공고와 겹치는 실적 N건 / 전체 M건」을 공고 × 신청기관 비교 화면에 표시 · 배포 완료 · `db8808e` · 겹친 낱말과 계획서로 가는 건수를 함께 보여 준다
- **다사113①** · 공고와 관련된 실적만 펼치고 나머지는 건수만 보내기 · 배포 완료 · `aa697f7` · 실적 99건일 때 기관 자료 10,337자 → 1,255자(이번 공고 기준). 값이 같아도 연도가 다르면 다른 줄로 두는 수정을 함께 넣음
- **다사112** · 보고 첫 줄에 작업번호 붙이는 규칙과 이 목록 만들기 · 배포 완료 · `1cd94db` · 다사113① 배포에 함께 올라감

## 아직 손대지 않은 것 — 늘 펼쳐진 빈 입력칸 (22-19 조사)

- 기관정보 화면 · 「기관자료 N건」 등록 폼 4칸 — 22-19와 성격이 같다
- 기관정보 화면 · 「아이디어·자산」 패널 6칸 (프로그램 구역 안)
- 신청기관 선택 화면 · 「이번 사업 전용 값」 3칸
- 검증·코칭 화면 · 「참고자료 N건」 5칸 — 자주 쓰는 길이라 두는 편이 낫다고 본다
- 관리자 화면 · 권한 지정 10칸, 프리미엄 이력 검색 4칸 — 급하지 않다

## 다음

- **22-02(새 번호)** — 요구사항 중복 세기. 「필수사업 다섯 가지를 모두 포함해야 한다」처럼 앞 항목을
  묶은 요약이 별개 요구로 한 번 더 세어진다. 조사 중이며 22-05가 들어와 잠시 멈췄다
