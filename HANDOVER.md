# ms12-proposal-maker 인수인계 문서

작성일: 2026-08-08 · 대상: 이 프로젝트를 처음 맡는 사람
이 문서에는 **비밀값(API 키·비밀번호)을 적지 않습니다.** 어디에 넣는지만 적었습니다.

---

## 1. 이 프로그램은 무엇인가

공모사업 **사업계획서를 만들고 검토해 주는 웹 프로그램**입니다. 단순한 AI 글쓰기 도구가 아니라 다음 흐름 전체를 돕습니다.

```
공고 가져오기 → 공고 확인 → 신청기관 선택 → 사업 선택·마스터 설계
→ 계획서 작성 → 검토·완성
                     ↕
              계획서 검증·코칭 (심사자 역할)
```

핵심 원칙 세 가지만 기억하면 됩니다.

1. **세 종류 정보를 절대 섞지 않는다**
   - 공모기관 정보(지원기관이 요구하는 것)
   - 신청기관 정보(우리 기관이 실제로 가진 것)
   - 이번 사업 정보(이번 공모를 위해 새로 정한 것)
   - 예: 기관이 평소 운영하는 프로그램이 12회기여도, 이번 공모가 16회기면 **기관 원본 12회기를 바꾸지 않습니다.**
2. **확인되지 않은 사실을 AI가 만들어내지 않는다** → 모르면 `[확인 필요]`로 남깁니다.
3. **여러 기관을 대행할 수 있는 구조** → 마인드스토리는 등록된 여러 신청기관 중 하나일 뿐입니다.

### 누가 쓰나

- 마인드스토리 내부 실무자(사업계획서 작성·검토 담당)
- 다른 기관의 계획서를 대행 작성·검토할 때도 사용
- 로그인 기능은 없습니다. 대신 브라우저마다 **복구키(UUID)** 를 만들어 그 키로 자기 자료보관함을 구분합니다. 복구키는 브라우저 `localStorage`에 저장되며, 잃어버리면 서버에서도 복구할 수 없습니다.

---

## 2. pro.ms12.org 와의 관계 · 배포 방법

- **pro.ms12.org 가 이 프로그램의 운영 주소**입니다. (2026-08-08 확인: 정상 200 응답, Cloudflare가 서비스 중)
- 호스팅은 **Cloudflare Pages**입니다. 정적 화면(`dist/`)과 서버 기능(`functions/api/*`)이 한 프로젝트에서 같이 돌아갑니다.
- 도메인 연결 정보는 `public/CNAME` 파일(`pro.ms12.org`)과 Cloudflare 대시보드의 Custom domain 설정에 있습니다.
- GitHub 저장소: `https://github.com/js94659535-stack/ms12-proposal-maker` (브랜치 `main`)

### 배포 순서

```powershell
npm install
npm test          # 전부 통과해야 함
npm run build     # dist/ 생성
git push origin main   # GitHub에 올리면 Cloudflare Pages가 자동 배포
```

- Cloudflare Pages 설정값: Build command `npm run build`, Build output `dist`, Root 저장소 루트
- 수동 배포도 가능: `npm run cf:deploy` (= `wrangler pages deploy dist`)

### 지금 배포 상태 (중요)

- **로컬에 아직 push하지 않은 커밋이 11개 있습니다.** 즉 pro.ms12.org에는 최신 기능이 아직 없습니다.
- 확인 방법: 운영 사이트의 JS 파일명과 로컬 `dist/index.html`의 JS 파일명이 다릅니다.
- 최신 기능을 운영에 반영하려면 `git push origin main` 이 필요합니다. (그동안 작업자가 "push는 하지 마라"고 지시해서 미룬 상태입니다.)

### 비밀값을 넣는 곳 (값은 여기 적지 않음)

| 이름 | 넣는 위치 | 용도 |
|---|---|---|
| `OPENAI_API_KEY` | Cloudflare 대시보드 → Workers & Pages → 프로젝트 → Settings → Variables and Secrets → **암호화 Secret** | OpenAI 호출 |
| `OPENAI_MODEL` | 같은 화면의 일반 Variable | 사용할 모델 ID |
| `OPENAI_PROBE_TOKEN` | 같은 화면의 Secret (선택) | 라이브 점검용. 없으면 점검 기능이 404를 돌려줍니다 |
| 자료보관함 복구키 | 사용자 브라우저 `localStorage`(`ms12_archive_key_v1`) | 사용자별 보관함 구분 |

**절대 하지 말 것**: 키를 `index.html`, `.env`, `wrangler.toml`, GitHub에 저장하지 마세요.

---

## 3. `C:\Users\UserK\Desktop\PLATFORM\ms12-proposal사업계획서모음` 폴더는 무엇인가

- 위치: `C:\Users\UserK\Desktop\PLATFORM\ms12-proposal사업계획서모음` (프로젝트 폴더 **바깥**, 형제 폴더)
- 내용: 실제로 제출했거나 참고용으로 모아둔 **과거 사업계획서·신청서 원본 6개** (hwp/pdf)
  - 예: 2025 마인드스토리 연혁, 2026년 복권기금 배분신청서, 미래내일 일경험 사업계획서, 수완아동센터 배분신청서 등
- **프로그램 코드가 이 폴더를 직접 읽지 않습니다.** 코드 어디에도 이 경로가 없습니다.
- 쓰임새: 사람이 손으로 참고하는 **자료 모음**입니다. 두 가지 방식으로 프로그램에 넣습니다.
  1. 「계획서 검증·코칭」 화면에 올려서 **검증 대상 계획서**로 사용
  2. 「신청기관 정보」 화면에 올려서 **기관 정보 추출 재료**로 사용 (실적·인력·시설·예산 경험 등)
- 주의: **HWP(한글 바이너리)는 프로그램이 읽지 못합니다.** 한/글에서 `다른 이름으로 저장 → HWPX / PDF / DOCX` 로 바꿔서 올려야 합니다. (자세한 이유는 6번 함정 참고)
- 이 폴더에는 기관 실제 정보가 들어 있으므로 외부 공유·업로드에 주의하세요.

---

## 4. 폴더 구조와 중요한 파일

```
ms12-proposal-maker/
├─ index.html            화면 껍데기 (실제 화면은 JS가 그림)
├─ src/                  브라우저에서 도는 코드
├─ functions/api/        Cloudflare Pages Functions (서버)
├─ migrations/           D1 데이터베이스 테이블 정의(SQL)
├─ test/                 테스트 (node --test)
├─ scripts/              수동 QA 스크립트
├─ dist/                 빌드 결과 (자동 생성, 손대지 말 것)
├─ docs/                 예전 GitHub Pages 배포 잔재 (지금은 안 씀)
├─ public/CNAME          도메인 이름 파일
├─ wrangler.toml         Cloudflare 설정 + D1 연결
└─ README.md             기능 설명서 (기능 추가 시 같이 갱신)
```

### 화면 쪽 (`src/`)

| 파일 | 줄수 | 하는 일 |
|---|---|---|
| `app.js` | 약 1,940 | **가장 큰 파일.** 상태 관리, 모든 화면 그리기, 버튼 연결. 화면 = 함수 하나(`~View()`), 클릭 = `bind()` 안에 등록 |
| `applicants.js` | 215 | 「신청기관 정보」 규칙 (항목·상태·영역·이력·출처별 보기) |
| `applicant-extract.js` | 195 | 문서에서 기관 정보 후보 뽑기 + 신규/동일/충돌 분류 |
| `coaching-handoff.js` | 218 | 검증·코칭 ↔ 계획서 쓰기 왕복, 버전 보관, 확정값 보호 |
| `reference-materials.js` | 133 | 참고자료(공고문·요강·평가기준) 판별 |
| `files.js` | 119 | 업로드 파일에서 글자 뽑기 (PDF/DOCX/TXT/HWPX) |
| `notices.js` `archive.js` `api.js` | 각 15~30 | 서버 호출 얇은 껍데기 |
| `fallback.js` | 49 | AI 없이 규칙만으로 분석·초안 만들기 |
| `export.js` | 53 | DOCX·PDF 내보내기 |
| `styles.css` | 1줄(압축) | 전체 스타일 |

### 서버 쪽 (`functions/api/`)

| 파일 | 하는 일 |
|---|---|
| `proposal.js` | 공고 분석 / 마스터 설계 / 분할 생성 / 항목 재작성 (OpenAI 호출) |
| `proposal-coaching.js` | 검증·코칭 (OpenAI background 방식) + 결과 검증·정규화 |
| `proposal-review.js` | 심사 검토 (8개 기준 채점) |
| `notices.js` | 사랑의열매 중앙회·광주지회 공고 목록·상세 가져오기 (외부 사이트 파싱) |
| `archive.js` | 자료보관함 (D1 읽기/쓰기: 공고·계획서·신청기관) |

### 데이터가 저장되는 곳

| 무엇 | 어디에 |
|---|---|
| 작업 중 상태(작성 중 계획서, 신청기관 목록 등) | 브라우저 `localStorage` (`ms12_project_v3`) |
| 수집한 공고 | D1 테이블 `archived_notices` (모두 공용) |
| 저장한 계획서(단계별·코칭 버전·수정본) | D1 테이블 `archived_proposals` (복구키별) |
| 신청기관 정보 | D1 테이블 `applicant_organizations` (복구키별) |

D1 데이터베이스 이름: `ms12-proposal-archive` (설정은 `wrangler.toml`)

---

## 5. 지금까지 만들어진 것 / 아직 안 된 것

### 만들어진 것 (동작 확인됨)

- 공고 가져오기(중앙회·광주지회), 공고 상세 확인, 누락 공고 URL 추가
- 자료보관함: 공고·계획서 저장/검색/다시 열기, 복구키로 기기 간 연결
- 신청기관 정보: 여러 기관 등록·선택·수정, 10개 영역, 항목별 `확인됨/확인 필요/오래된 정보`
- 확인된 기관 정보만 AI 작성 요청에 전달(확인 안 된 값은 전달 안 함)
- 이번 사업 전용 값이 기관 원본을 바꾸지 않음
- 문서(업로드·붙여넣기·검증한 계획서·보관함 계획서)에서 기관 정보 **후보** 만들기 + 기준시점·출처·근거문장 보존 + 이력 남기기
- 계획서 작성: 마스터 설계 → 분할 생성 → 조립, 항목별 재작성
- 심사 검토(8기준), 검증·코칭(문제별 위치·이유·개선방향·근거)
- 검증·코칭 ↔ 계획서 쓰기 **왕복**: 문제 전달 → 수정 → 버전 저장(V1, V2…) → 다시 검증 → 해결/잔존/신규 비교
- 외부에서 가져온 계획서도 작업본으로 전환해 같은 왕복 사용(원본 보존)
- 참고자료 분리 입력 + 자료 판별(공식 근거/참고용/맞지 않음/출처확인필요/충돌)
- HWPX 직접 읽기, 드래그앤드롭 업로드, AI 작업 경과시간 표시
- 테스트 122개 전부 통과, `npm run build` 통과

### 아직 안 된 것 / 다음 할 일

1. **운영 배포 반영** — 커밋 11개 push 안 됨 (가장 급함)
2. **운영 D1 마이그레이션 확인** — `migrations/0002_applicants.sql`(신청기관 테이블)이 운영 D1에 적용됐는지 확인 필요. 로컬만 적용된 상태로 진행했습니다.
   ```powershell
   npx wrangler d1 migrations apply ms12-proposal-archive --remote
   ```
3. **HWP(바이너리) 직접 읽기** — 지원 안 함. 변환 안내로 처리 중 (이 PC에만 .hwp 약 1,995개 / .hwpx 약 516개라 실제 영향이 큼)
4. **화면 클릭 단위 실사용 점검** — 자동 브라우저 도구가 없어 사람이 직접 눌러 확인해야 하는 항목이 남아 있음
5. **공개 우수 사업계획서 자료 활용** — 방향만 정해져 있고 미구현 (크롤러는 만들지 않기로 함)
6. **기관 정보 추출 규칙 보강** — 지금은 보수적인 규칙 기반. 문장형 문서에서 놓치는 정보가 있을 수 있음

---

## 6. 최근 커밋 20개 (한국어 한 줄 설명)

| 해시 | 날짜 | 내용 |
|---|---|---|
| `219ac80` | 08-08 | 보관함의 과거 계획서로 신청기관 정보 채우기(출처별 보기·실적 연도순 포함) |
| `ca4e7b5` | 08-08 | 코칭 결과 422 원인 해결: 위험등급 불일치를 버리지 않고 정규화 |
| `009f4c5` | 08-08 | 참고자료를 계획서와 분리 입력 + 자료 신뢰도 판별 |
| `83242ba` | 08-08 | 검증한 계획서에서 신청기관 정보 뽑기(개인정보 제외) |
| `f7e6100` | 08-08 | HWPX 직접 읽기, HWP는 변환 안내 |
| `24f4e1f` | 08-08 | 신청기관 없어도 작성 진행 가능 + 공고 목록/보관함 구분 표시 |
| `3de1e87` | 08-08 | 외부 계획서도 작업본으로 전환해 왕복 사용(원본 보존) |
| `a68fee8` | 08-08 | 계획서 쓰기 ↔ 검증·코칭 왕복 연결(버전·확정값 보호) |
| `9d5981a` | 08-08 | 기관 문서에서 정보 업데이트 후보 만들기(기준시점·이력) |
| `6d14d7c` | 08-08 | 「신청기관 정보」 기능 신설(여러 기관 등록·선택) |
| `dd73daf` | 08-07 | 확인정보 이후 전체 흐름 fixture QA 추가 |
| `761d6b3` | 08-07 | 마스터 설계 응답 시간 단축 |
| `39c3ce9` | 08-07 | 코칭 문제 심각도 기준 정리 |
| `ec19ed9` | 08-07 | 문제별 코칭 QA 실행 가능하게 함 |
| `a137ebf` | 08-07 | 코칭 과잉진단(없는 문제 만들기) 줄임 |
| `19d9f6d` | 08-07 | AI 작업 경과시간 표시 |
| `3131bba` | 08-07 | 코칭 근거 검증 완화 + 타이머 추가 |
| `d88aa24` | 08-07 | 코칭 검증 실패 원인 진단 정보 수정 |
| `3c64ece` | 08-07 | 코칭 완료 처리 흐름 단순화 |
| `eed5983` | 08-07 | 코칭 품질 검증 완성 |

전체 커밋 수: 64개

---

## 7. 겪었던 함정 · 조심할 것

### 개발 환경

1. **`npm run dev`(vite)로는 공고 가져오기·AI 기능이 안 됩니다.**
   vite는 `functions/` 폴더의 서버 기능을 서비스하지 않아 `/api/*`가 404가 납니다.
   서버 기능까지 시험하려면:
   ```powershell
   npm run build
   npm run cf:dev      # http://127.0.0.1:8788
   ```
2. **로컬 D1과 운영 D1은 다른 데이터베이스입니다.** `wrangler pages dev`는 로컬 D1을 씁니다. 운영 데이터에 `--remote`로 직접 손대지 마세요.
3. 테스트는 **실제 OpenAI를 호출하지 않습니다.** 전부 fixture(가짜 자료)와 mock으로 돌아갑니다. 이 원칙을 깨지 마세요(비용·안전).

### 자주 문제가 됐던 지점

4. **코칭 결과 422 오류** — OpenAI는 200인데 우리 검증에서 반려되던 문제. 원인은 `riskType`(중대 위험 종류)과 `priority`(등급)가 어긋난 것. 지금은 `normalizeUnsupportedCriticalIssues()`가 **근거가 확실하면 최우선 경고로 올리고, 근거가 약하면 일반 등급으로 낮춰** 정상 결과를 버리지 않습니다. 단 **schema 오류와 근거 위조는 계속 차단**합니다. 이 균형을 깨지 마세요.
5. **과잉진단 방지 규칙** — "정보가 부족하다"는 이유만으로 제출불가·예산위반으로 단정하지 않습니다. 관련 테스트(`정상 계획서의 근거 부족을 과잉진단하지 않는다`)를 반드시 통과시키세요.
6. **HWP(바이너리)** — 브라우저에서 안정적으로 읽을 수 없어 지원하지 않기로 결정했습니다(암호화된 배포용 문서 문제 포함). HWPX는 ZIP+XML이라 외부 라이브러리 없이 읽습니다. 억지로 HWP 파서를 붙이지 마세요.
7. **과거 문서로 현재값 덮어쓰기 금지** — 기관 정보는 반드시 `신규 / 동일 / 변경 가능성 / 충돌 / 누적 추가 / 이전 시점 정보`로 분류만 하고, 사용자가 누를 때만 반영합니다. 반영해도 기존 값은 `history`에 남깁니다. 사업실적은 덮어쓰지 않고 **연도별로 쌓습니다.**
8. **개인정보 수집 금지** — 전화·이메일·주민번호·생년월일이 들어간 문장은 기관 정보 후보로 만들지 않습니다(`PERSONAL_INFO_PATTERN`).
9. **확정 수치 보호** — 코칭 반영 수정에서 인원·회기·기간·예산 숫자가 바뀌면 수정안을 적용하지 않습니다(`verifyLockedValues`).
10. **공고 목록은 느립니다(약 11초).** 각 공고의 상세를 함께 읽기 때문입니다. 멈춘 게 아니니 경과시간 표시를 보세요.
11. **마감 지난 공고는 목록에서 자동 제외**됩니다. 목록이 비어 보이면 고장이 아니라 마감일 문제일 수 있습니다.
12. **`localStorage` 용량** — 참고자료 원문 등이 커지면 저장 한도를 넘을 수 있어, 저장 실패해도 화면이 멈추지 않게 예외 처리해 두었습니다. 큰 원문을 상태에 계속 쌓지 마세요.
13. **`app.js`가 큽니다(약 1,940줄).** 대규모 리팩터링은 하지 않기로 했습니다. 새 기능은 작은 순수 모듈(`src/*.js`)로 만들고 `app.js`에서는 화면·연결만 하세요.
14. **`docs/` 폴더는 옛날 배포 잔재**입니다. 지금 배포와 무관하니 건드리지 마세요.

---

## 8. 지금 진행 중이던 일과 진척

마지막 작업(커밋 `219ac80`)까지 **요청받은 범위는 모두 끝난 상태**입니다. 미해결 과제만 남아 있습니다.

| 항목 | 상태 |
|---|---|
| 보관함 과거 계획서 → 신청기관 정보 반영 | 완료 (테스트 4건 추가, 저장·재조회·기관 격리 확인) |
| 코칭 422 정규화 | 완료 (fixture 2방향 확인) |
| 참고자료 분리·판별 | 완료 |
| HWPX 지원 | 완료 (실제 HWPX 2개로 검증) |
| 검증·코칭 ↔ 계획서 쓰기 왕복 | 완료 (fixture E2E) |
| **화면에서 사람이 직접 눌러보는 확인** | **미완** — 브라우저 자동화 도구가 없어 미실행 |
| **운영 push·배포** | **미완** — 커밋 11개 대기 |
| **운영 D1에 0002 마이그레이션 적용** | **미확인** |

### 바로 이어서 할 일 (순서 추천)

1. `npm test` → `npm run build` 확인
2. `npm run cf:dev`로 로컬에서 아래를 사람이 직접 눌러 확인
   - 공고 가져오기 → 공고 확인
   - 신청기관 등록 → 문서/보관함에서 정보 추출 → 반영 → 새로고침 후에도 남는지
   - 검증·코칭 → 문제 선택 → 계획서 쓰기에서 수정 → 버전 저장 → 다시 검증
3. 운영 D1에 `0002_applicants.sql` 적용 여부 확인, 필요하면 적용
4. `git push origin main` → Cloudflare 자동 배포 → pro.ms12.org에서 재확인

---

## 9. 개발 시작하기 (명령어 모음)

```powershell
cd C:\Users\UserK\Desktop\PLATFORM\ms12-proposal-maker
npm install

npm test            # 테스트 122개 (실제 AI 호출 없음)
npm run build       # dist/ 빌드

npm run dev         # 화면만 빠르게 볼 때 (⚠ /api 는 동작 안 함)
npm run cf:dev      # 서버 기능까지 포함 (http://127.0.0.1:8788)

npx wrangler d1 migrations apply ms12-proposal-archive --local    # 로컬 DB 준비
```

권장 환경: Node 24, wrangler 4.x, Windows PowerShell

### 코드 수정 시 지켜온 규칙

- 한 번에 목표 1개, 수정 파일 최소화, 대규모 리팩터링 금지
- 이미 통과한 기능은 다시 쓰지 않기
- 새 기능은 **순수 모듈 + 테스트**로 만들고 `app.js`에서 연결만
- 테스트는 fixture만 사용, 실제 OpenAI 호출 금지
- 기능을 바꾸면 `README.md`도 같이 고치기
- 커밋은 의미 단위로, push는 배포를 뜻하므로 확인 후에
